// src/App.tsx
// This file is intentionally empty. It is the default Vite scaffold entry point
// and is NOT used by the extension. The actual extension UI lives in:
//   - src/popup/App.tsx  → popup window
//   - src/content/       → in-page popover (shadow DOM)
//   - src/background/    → service worker

// If you see this file referenced in a build, something is misconfigured.

export {};
